import React from 'react';

import {
    Typography,
} from "@material-tailwind/react";

function Events() {
    return (
        <div>
            {/* Placeholder content for Users. Update this component to your liking. */}
            <Typography variant="h5" color="blue-gray">
                Events List
            </Typography>
            {/* Maybe a list or table showing users can go here */}
        </div>
    );
}

export default Events;
